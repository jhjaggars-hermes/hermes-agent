# Seed .env for OpenShell sandbox deployment.
# Copied to /sandbox/.env by workspace-init on first boot.
# Non-secret config values only — credentials come from OpenShell providers.
DISCORD_ALLOWED_USERS=203588743326597120
API_SERVER_ENABLED=false
